import { FETCH_RANDOM_NAMES } from '../actions/types.js'

const initialState = {
    names: []
}

export default function(state = initialState, action) {
    switch(action.type) {
        default:
            return state
    }
}